import { MedusaService } from "@medusajs/framework/utils"

class AdminProductsService extends MedusaService({}) {
  // This module uses Medusa's built-in product module
  // It just provides additional admin API routes
}

export default AdminProductsService